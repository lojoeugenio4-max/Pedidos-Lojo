/**
 * Interruptores generales de funcionalidades.
 *
 * Todos permanecen desactivados mientras desarrollamos.
 * Cambiarlos a true requiere una activación deliberada.
 */
export const FEATURES = Object.freeze({
  promotionsEngine: false,
  bingo: false,
  customerPromotionsMenu: false,
  customerIdentityLinks: false,
});